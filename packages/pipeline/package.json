{
  "name": "@btc/pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Scripts that fetch, extract, derive and validate the sourced data for Be the Chancellor",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "fetch": "tsx src/fetch.ts",
    "validate": "tsx src/validate.ts",
    "derive": "tsx src/derive.ts",
    "check-drift": "tsx src/check-drift.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@btc/engine": "*",
    "exceljs": "^4.4.0",
    "fast-xml-parser": "^5.0.0",
    "fflate": "^0.8.2",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "vitest": "^4.0.0"
  }
}
